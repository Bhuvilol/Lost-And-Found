import React from 'react';

const ExpenseChart = ({ expenses }) => (
  <div>
    <h3>Expense Chart</h3>
    <p>Graph will appear here (Chart.js integration pending).</p>
  </div>
);

export default ExpenseChart; 